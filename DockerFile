FROM ubuntu:latest

RUN apt-get update && \ apt-get install python3 -y && \ apt-get install vim -y && \ apt-get install zip -y && \ apt-get install unzip -y

COPY zip_job.py . /tmp/
RUN chmod +x /tmp/zip_job.py
WORKDIR /tmp
CMD ["python3" , "./zip_job.py]

RUN uname -a
RUN test -f /tmp/zip_job.py && echo "file exists"

ENV VERSION=1.2.0
